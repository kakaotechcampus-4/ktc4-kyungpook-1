# 프론트엔드 트러블슈팅

## 2026-09-26 — 고정 폭과 모바일 예외의 충돌

- 작성 메타데이터의 inline 160/220px와 최소 240px가 모바일에서 다른 줄·다른 폭을 만들었다. 명명한 grid 클래스로 교체하고 제목 전체 행, 나머지 동일 폭을 검증했다.
- 안내문 제목의 flex-shrink:0이 모바일 내용 폭을 압축했다. 모바일은 제목/본문을 세로로 배치한다.
- 모달 최대 높이가 모바일 폭에만 적용돼 844×390 가로 화면에서 하단 버튼이 화면 밖에 있었다. 모든 폭에 최대 높이와 본문 스크롤을 적용하고 E2E로 버튼 경계를 확인한다.
- 스크린샷은 애니메이션이 끝난 뒤 촬영한다. 이전 즉시 캡처의 투명 모달은 fade 애니메이션 중간 프레임이었다.

## 2026-09-26 — Job 재시도·후보 변경 성공 시점

- FAILED 분기의 `retryable !== false`는 null을 허용했다. 부분 완료와 동일한 `=== true` 정책을 공통 훅으로 묶고, 최초 렌더와 Job 교체 시에도 대기 시간을 즉시 계산한다.
- 후보 제외는 서버 응답 전에 선택 해제와 성공 토스트가 발생했다. 실패 주입 E2E로 재현 후 성공 콜백으로 옮겼다. 카드 생성/직접 추가의 reject도 처리해 화면과 입력을 유지한다.
- 후보 보드 재진입 후 잘못된 저장소의 선택을 저장하지 않도록 repoId별 컴포넌트 상태를 초기화하고, 선택 저장값의 배열/문자열 타입을 검사한다.
- 공유 JSON을 `new URL(..., import.meta.url)`로 읽으면 Vite 변환으로 file URL이 아닌 값이 생겼다. 테스트의 프로젝트 루트에서 파일 경로를 계산해 원본 fixture를 읽는다.
- Node 24.14.1의 기존 AbortSignal realm 오류는 #57에서 정한 Node 22 기준과 비차단 24 matrix를 유지하며 추적한다. 전역 polyfill로 동작을 바꾸지 않는다.

## 2026-09-10 — 모바일 단일 열 규칙과 데스크톱 본문 폭이 적용되지 않음

| 항목 | 내용 |
| --- | --- |
| 증상 | 모바일에서도 앱 셸이 2열처럼 계산되고, 넓은 데스크톱에서 본문이 1180px에 멈췄다. 랜딩 헤더와 본문의 시작선도 일치하지 않았다. |
| 영향 | 화면 오른쪽에 불필요한 빈 공간이 생기고 모바일 메뉴의 이름이 대부분 숨었다. 화면별 정보 우선순위가 약해졌다. |
| 확인한 사실 | `main.tsx`에서 `shell.css` 뒤에 `layout-tio.css`가 로드됐다. 뒤 파일의 `.shell`과 `.main` 규칙이 앞 파일의 모바일 미디어 쿼리와 최대 폭을 덮어썼다. |
| 조치 | 모든 기존 스타일 뒤에 `responsive.css`를 로드해 셸과 breakpoint의 최종 책임을 한 파일에 모았다. 767px 이하는 상단 브랜드와 이름이 있는 하단 메뉴, 768–1199px은 축소 사이드바, 1200px 이상은 전체 작업 폭을 사용한다. |
| 재발 방지 | Playwright가 320, 390, 768, 1024, 1440, 1920px에서 주요 라우트의 `scrollWidth`와 내비게이션을 검사한다. 새 전역 레이아웃 규칙은 `responsive.css`에 추가하고 이 파일의 로드 순서를 마지막으로 유지한다. |

## 반응형 화면 확인

```bash
cd frontend
npm install
npm run typecheck
npm test
npm run e2e
npm run e2e:static
```

브라우저에서는 최소 320px 로그인, 390px 홈·인터뷰, 768px 저장소 선택, 1024px 카드 상세, 1440px과 1920px 카드 목록을 확인한다. 고정 하단 메뉴와 화면 내부의 sticky footer가 서로 가리지 않는지도 함께 확인한다.

이번 변경에서는 2026-09-10 기준으로 타입 검사와 프로덕션 빌드가 완료됐고, Vitest 12개와 개발·정적 빌드 Playwright E2E 각각 18개가 통과했다. PC 랜딩에서는 헤더와 본문의 좌우 경계, 로고와 상단 CTA 높이의 차이를 각각 1px 이하로 검사한다.

## 보안 기록 원칙

오류 재현 자료와 문서에는 `.env`, API 키, OAuth 토큰, 세션 쿠키를 넣지 않는다. 한 번 Git 기록에 들어간 비밀은 파일을 지워도 남으므로 발견 즉시 키를 폐기하고 담당자에게 알린다.

## 2026-09-10 — 프로필 공백과 반복 카드로 정보 위계가 약함

| 항목 | 내용 |
| --- | --- |
| 증상 | 데모 사용자의 프로필이 빈 원으로 보였고, STAR가 작은 네모 네 개로만 표시되어 필드 의미와 정렬을 읽기 어려웠다. 경험·저장소·후보·통계 항목마다 둥근 카드와 그림자가 반복됐다. |
| 영향 | 중요한 제목과 상태보다 컨테이너 외곽선이 먼저 보였고, 모바일에서는 화면이 조밀하게 느껴졌다. |
| 조치 | 로컬 프로필 SVG와 오류 시 이니셜 fallback을 가진 `UserAvatar`를 공통 사용한다. STAR는 고정 네 열과 한국어 라벨을 사용한다. 반복 항목은 `experience-list`와 `record-list` 하나로 묶고 20–24px 행 간격과 구분선으로 나눈다. |
| 보존한 경계 | 선택, 확인 필요, 실패, 입력, 모달, sticky footer는 기능과 상태를 알아야 하므로 기존 경계를 유지한다. |
| 재발 방지 | Vitest가 아바타 성공·실패와 STAR 순서를 검사하고, Playwright가 목록 묶음 수, 그림자 제거, STAR 동일 너비 및 전체 breakpoint를 검사한다. |

## 백엔드·AI 계약 연동에서 걸린 것들 (2026-09)

### 목이 `fetch` 를 가로채서 Playwright 의 `waitForResponse` 가 안 잡힌다
데모 모드는 `window.fetch` 자체를 갈아 끼우므로 네트워크 요청이 아예 발생하지 않는다.
"서버에 저장됐는지"를 네트워크 이벤트로 확인하려던 테스트가 15초 타임아웃으로 죽었다.
→ 화면을 떠났다 돌아와도 값이 남아 있는지로 확인한다. 브라우저 저장이면 통과할 수 없는 조건이라 증명력은 같다.

### E2E 헬퍼가 매 페이지 로드마다 목 DB 를 지워 "새로고침 복구"를 검증할 수 없었다
`addInitScript` 가 로드마다 도는 탓에, 새로고침하면 복구할 상태 자체가 사라졌다.
→ `gitory.e2e.seeded` 표시를 두고 **테스트당 첫 로드에서만** 시드로 되돌린다.
브라우저 컨텍스트가 테스트마다 새로 뜨므로 표시도 같이 사라져, 테스트 간 격리는 그대로다.

### 목이 Job 을 메모리에만 들고 있었다
`snapshot()` 이 `Map(jobs)` 을 뺐기 때문에 새로고침하면 진행 중이던 분석이 통째로 사라졌다.
실서버에서는 Job 이 서버에 있으니 이건 목이 실제와 다르게 구는 자리였다.
→ 스냅샷에 `jobs` 와 `idem` 을 같이 담는다. 복구 화면을 데모에서도 그대로 확인할 수 있다.

### `CardSummary` 에 프론트 전용 필드가 남아 있었다
백엔드가 확정한 카드 목록 응답에는 `evidenceCount` 같은 필드가 없다. 필수로 두면 붙이는 날 첫 호출에서 `ContractError` 가 난다.
→ 계약에 있는 필드만 필수로 두고, 목록을 읽기 좋게 하는 부가 필드는 zod `default()` 로 내렸다.


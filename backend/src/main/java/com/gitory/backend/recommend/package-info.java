/**
 * <b>추천 엔진</b> — 성과 크기·본인 기여도·근거 밀도로 후보를 뽑고 순위를 매긴다.
 *
 * <p>입력: 근거 묶음 · 출력: 순위가 있는 후보와 추천 이유
 *
 * <h2>경계 규칙</h2>
 * <ul>
 *   <li><b>규칙으로 끊는다. LLM 을 호출하지 않는다.</b> 팀·개인 판정은 552토큰
 *       메타데이터만으로 3/3 정답이 나왔고, 근거로 인용한 값이 전부 정수·문자열이었다.
 *       모델에 맡기면 비용과 지연만 늘고 재현성이 떨어진다.</li>
 *   <li>추천 이유({@code reason})를 쓸 수 없는 후보는 <b>올리지 않는다</b>
 *       (api-spec.md §4). 근거 0개 카드가 화면에 뜨면 안 된다.</li>
 *   <li>후보 0개는 실패가 아니라 판정이다. {@code verdict: "EMPTY"} + 200 으로 내린다.</li>
 * </ul>
 *
 * <h2>프론트가 열어 둔 질문에 대한 답</h2>
 * <ul>
 *   <li><b>{@code lowCardWorth} 는 규칙이다</b>(api-spec.md 미확정 #3).
 *       문서·설정 전용 변경, 변경량 임계 미만, 근거 커밋 1건 — 전부 정수 비교다.</li>
 *   <li><b>{@code myCommitCount}/{@code teamCommitCount} 는 여기서 센다</b>(미확정 #4).
 *       봇 제외 · 머지 커밋 제외 · 브랜치 포함이 세 함정이다. login 이 없으면
 *       author name 으로 폴백하되 대소문자를 정규화한다 — 정규화를 빠뜨리면
 *       본인 커밋이 조용히 0개가 된다.</li>
 * </ul>
 */
package com.gitory.backend.recommend;

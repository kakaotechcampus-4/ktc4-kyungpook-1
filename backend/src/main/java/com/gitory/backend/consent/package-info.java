/**
 * <b>연결·동의 경계</b> — 권한 범위, 선택 범위, 철회 상태를 검증한다.
 *
 * <p>입력: GitHub OAuth 결과, 사용자의 저장소 선택 · 출력: 최소 권한 세션, 분석 범위
 *
 * <h2>경계 규칙</h2>
 * <ul>
 *   <li>다른 어떤 모듈도 "이 사용자가 이 저장소를 분석해도 되는가"를 스스로 판단하지 않는다.
 *       전부 이 모듈에 묻는다. ADR-0003 이 지켜지는 단일 지점이다.</li>
 *   <li>OAuth 토큰은 이 모듈 밖으로 나가지 않는다. {@code ingest} 에는 토큰이 아니라
 *       "이 범위로 호출할 수 있는 클라이언트"를 넘긴다 — 토큰이 로그·프롬프트·응답에
 *       실릴 경로를 타입으로 막는다(security-privacy.md).</li>
 *   <li>연결 해제는 토큰 폐기 + 후속 수집 중단 + 삭제 작업 등록까지가 한 트랜잭션이다
 *       (api-data-assumptions.md 계약 원칙 6: 삭제 전파).</li>
 * </ul>
 */
package com.gitory.backend.consent;
